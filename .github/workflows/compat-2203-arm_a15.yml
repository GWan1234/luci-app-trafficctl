name: OpenWrt 22.03 / arm_a15

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/_compat-impl.yml
    with:
      image: ghcr.io/openwrt/rootfs:arm_cortex-a15_neon-vfpv4-v22.03.4
      platform: linux/arm/v7
